/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ['https://atdfzrqciomceoshlujd.supabase.co'],
  }
};

export default nextConfig;
